#include "main.h"
#include <stdlib.h>
/**
 * *_abs - Prints the absolute of an inputted value
 * *@n: the inputted value;
 * *Return: returns the absolute value of the integer
 */
int _abs(int n)
{
	return (abs(n));
}
